diff --git a/api/routes/research.ts b/api/routes/research.ts
index c6a4535..3f38a13 100644
--- a/api/routes/research.ts
+++ b/api/routes/research.ts
@@ -79,7 +79,7 @@ researchRoutes.get('/info', (_req, res) => {
       'GET /v1/research/info': 'This endpoint — dataset schema and repo info',
       'GET /v1/research/stats': 'Aggregate stats across all published HF batches',
       'GET /v1/research/batches': 'List all published batch files in the HF repo',
-      'GET /v1/research/batch/*': 'Read a specific batch file (JSONL parsed)',
+      'GET /v1/research/batch/:category/:filename': 'Read a specific batch file (JSONL parsed)',
       'GET /v1/research/query': 'Query the full corpus with filters (category, model, mode, time range)',
       'POST /v1/research/flush': 'Force-flush in-memory buffers to HuggingFace',
       'GET /v1/research/download': 'Download full corpus as streaming JSONL',
@@ -154,11 +154,13 @@ researchRoutes.get('/batches', async (req, res) => {
 })
 
 // ── Batch: read a single file ────────────────────────────────────────
-// Path comes as wildcard: /v1/research/batch/metadata/batch_2024-01-01_0001.jsonl
+// Example path: /v1/research/batch/metadata/batch_2024-01-01_0001.jsonl
 
-researchRoutes.get('/batch/*', async (req, res) => {
+researchRoutes.get('/batch/:category/:filename', async (req, res) => {
   try {
-    const filePath = req.params[0]
+    const category = req.params.category
+    const filename = req.params.filename
+    const filePath = category + '/' + filename
     if (!filePath || !filePath.endsWith('.jsonl')) {
       res.status(400).json({ error: 'Invalid batch path. Expected: metadata/batch_*.jsonl or dataset/batch_*.jsonl' })
       return
diff --git a/api/server.ts b/api/server.ts
index 1479d30..8102bd7 100644
--- a/api/server.ts
+++ b/api/server.ts
@@ -113,7 +113,7 @@ app.get('/v1/info', (_req, res) => {
       'GET  /v1/research/info': 'Research dataset schema, repo info (Pro+)',
       'GET  /v1/research/stats': 'Aggregate stats across all published HF batches (Pro+)',
       'GET  /v1/research/batches': 'List all published batch files (Pro+)',
-      'GET  /v1/research/batch/*': 'Read a specific batch file (Pro+)',
+      'GET  /v1/research/batch/:category/:filename': 'Read a specific batch file (Pro+)',
       'GET  /v1/research/query': 'Query the full corpus with filters (Enterprise)',
       'POST /v1/research/flush': 'Force-flush in-memory buffers to HuggingFace (Enterprise)',
       'GET  /v1/research/download': 'Download full corpus as streaming JSONL (Enterprise)',
